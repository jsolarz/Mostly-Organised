{
  "name": "mostly-organised",
  "version": "0.1.0",
  "description": "Mostly Organised — Google Docs TOC add-on: drag-to-reorder, numbering, promote/demote",
  "private": true,
  "scripts": {
    "dev": "node scripts/watch.js",
    "build": "node scripts/build.js",
    "push": "npm run build && clasp push",
    "push:watch": "clasp push --watch",
    "lint": "eslint src/**/*.js"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "chokidar": "^3.6.0",
    "eslint": "^8.57.0"
  }
}
